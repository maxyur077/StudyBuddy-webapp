import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-rstpass',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './rstpass.component.html',
  styleUrls: ['./rstpass.component.scss'],
})
export class RstpassComponent {}
